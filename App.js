import React from "react";
import ReactDOM from "react-dom";

const jsxHeading = <h1>Namaste React using JSX</h1>;
console.log(jsxHeading);

const root = document.getElementById("root");

ReactDOM.render(jsxHeading, root);
